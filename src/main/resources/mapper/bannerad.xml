<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.bookhub.repository.BannerAdRepository">

    <insert id="insert">

        insert into bh_banner(title, content, writer, origin_file_name, upload_file_name, post_yn, created_at)
        values(#{title}, #{content}, #{writer}, #{originFileName}, #{uploadFileName}, #{postYn}, now())

    </insert>


    <select id="findAllWithPagingAndWriter"
            resultType="com.library.bookhub.entity.BannerAd">
        SELECT * FROM bh_banner WHERE LOWER(writer) LIKE CONCAT('%',
        LOWER(#{writer}),
        '%') LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="getAdTotalCount" resultType="int">
        SELECT COUNT(*) FROM
        bh_banner;
    </select>


</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.library.bookhub.repository.BannerAdRepository">

	<insert id="insert">

		INSERT INTO BH_BANNER(title, content, writer, origin_file_name,
		upload_file_name, post_yn, created_at)
		VALUES(#{title}, #{content}, #{writer}, #{originFileName}, #{uploadFileName},
		#{postYn}, now())

	</insert>


	<select id="findAllWithPagingAndWriter"
		resultType="com.library.bookhub.entity.BannerAd">
		SELECT * FROM BH_BANNER WHERE LOWER(writer) LIKE CONCAT('%', LOWER(#{writer}), '%') LIMIT #{limit} OFFSET #{offset}

	</select>

	<select id="getAdTotalCount" resultType="int">
		SELECT COUNT(*) FROM
		BH_BANNER
	</select>


	<select id="findById"
		resultType="com.library.bookhub.entity.BannerAd">
		SELECT ID ,UPLOAD_FILE_NAME, CONTENT FROM BH_BANNER
		WHERE POST_YN = 'N'
	</select>


	<select id="findByBannerId" parameterType="int"
		resultType="com.library.bookhub.entity.BannerAd">
		SELECT *
		FROM BH_BANNER
		WHERE id = #{id}
	</select>





	<update id="update"
		parameterType="com.library.bookhub.entity.BannerAd">
		UPDATE BH_BANNER
		SET
		title = #{title},
		content = #{content},
		writer = #{writer},
		origin_file_name = #{originFileName},
		upload_file_name = #{uploadFileName},
		post_yn = #{postYn},
		created_at = now()
		WHERE id = #{id}
	</update>


	<update id="updatePostStatus"
		parameterType="com.library.bookhub.entity.BannerAd">
		UPDATE BH_BANNER
		SET
    		post_yn = #{postYn}
		WHERE
    		id = #{id}
	</update>



	<delete id="deleteById" parameterType="int">
		DELETE FROM BH_BANNER
		WHERE id = #{id}
	</delete>


	<select id="existById" parameterType="int" resultType="long">
		SELECT COUNT(id)
		FROM BH_BANNER
		WHERE id = #{id}
	</select>

</mapper>

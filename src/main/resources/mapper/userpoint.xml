<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.bookhub.repository.UserPointRepository">

<select id="findAll" resultType="com.library.bookhub.entity.UserPoint">
select * from bh_user_point

</select>

<insert id="save" parameterType="com.library.bookhub.entity.UserPoint">
insert into bh_user_point(user_id, point_name, purchase_date, point, imp_uid, merchant_uid)
values(#{userId}, #{pointName}, #{purchaseDate}, #{point}, #{impUid}, #{merchantUid})
</insert>

</mapper>
